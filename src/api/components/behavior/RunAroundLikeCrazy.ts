/**
 * Allows the mob to run around aimlessly.
 */
declare interface RunAroundLikeCrazy {

	/**
	 * Movement speed multiplier of the mob when using this AI Goal.
	 * @default 1.0
	 */
	speed_multiplier: number;
}
