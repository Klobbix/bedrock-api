/**
 * Allows the mob to follow their parent around.
 */
declare interface FollowParent {

	/**
	 *  Movement speed multiplier of the mob when using this AI Goal.
	 *  @default 1.0
	 */
	speed_multiplier: number;
}
