/**
 * Allows the mob to run away from direct sunlight and seek shade.
 */
declare interface FleeSun {

	/**
	 * Movement speed multiplier of the mob when using this AI Goal.
	 * @default 1.0
	 */
	speed_multiplier: number;
}
