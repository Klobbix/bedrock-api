/**
 * Sets the offset from the ground that the entity is actually at.
 */
declare interface GroundOffsetComponent {

	/**
	 * The value of the entity's offset from the terrain, in blocks.
	 * @default 0.0
	 */
	value: number;
}
