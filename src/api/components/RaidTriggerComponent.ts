/**
 * Attempts to trigger a raid at the entity's location.
 */
declare interface RaidTriggerComponent {

	/**
	 * Event to run we attempt to trigger a raid on the village.
	 */
	triggered_event: string;
}
