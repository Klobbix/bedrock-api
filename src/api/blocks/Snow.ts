export class Snow {
	/**
	 * The number of layers in addition to the bottom layer.
	 * Allowed values: 0-7.
	 * @default 0.
	 */
	height: number = 0;

	/**
	 * No description.
	 * @default 0.
	 */
	covered_bit: number = 0;
}
