import { Directional } from "./data/Directional";

export class Loom implements Directional {

	/**
	 * The direction the loom is facing.
	 * Opposite from the direction the player faces when placing a loom.
	 */
	direction: number = 0;

}
